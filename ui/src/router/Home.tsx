import React, { ReactElement } from 'react'

interface Props {
    path: string
}

const Home: React.FC<Props> = ({}): ReactElement => {
    return (
        <div>
            <p>Home</p>
        </div>
    )
}

export default Home
